<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_gradient"
    android:orientation="vertical"
    android:padding="@dimen/screen_padding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        tools:ignore="ButtonStyle">

        <TextView
            android:id="@+id/settings_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/settings"
            android:textColor="@color/text_primary"
            android:textSize="30sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/save"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:background="@drawable/tile_selector"
            android:nextFocusDown="@+id/welcome_text"
            android:nextFocusRight="@+id/web_shortcuts"
            android:paddingHorizontal="28dp"
            android:text="@string/save"
            android:textAllCaps="false"
            android:textColor="@color/text_primary" />

        <Button
            android:id="@+id/web_shortcuts"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:layout_marginStart="18dp"
            android:background="@drawable/tile_selector"
            android:nextFocusDown="@id/welcome_text"
            android:nextFocusLeft="@id/save"
            android:nextFocusRight="@id/open_android_settings"
            android:paddingHorizontal="20dp"
            android:text="@string/web_shortcuts"
            android:textAllCaps="false"
            android:textColor="@color/text_primary" />

        <Button
            android:id="@+id/open_android_settings"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:layout_marginStart="18dp"
            android:background="@drawable/tile_selector"
            android:nextFocusDown="@id/welcome_text"
            android:nextFocusLeft="@id/web_shortcuts"
            android:nextFocusRight="@+id/build_info"
            android:paddingHorizontal="20dp"
            android:text="@string/open_android_settings"
            android:textAllCaps="false"
            android:textColor="@color/text_primary" />

        <Button
            android:id="@+id/build_info"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:layout_marginStart="18dp"
            android:background="@drawable/tile_selector"
            android:nextFocusDown="@id/welcome_text"
            android:nextFocusLeft="@id/open_android_settings"
            android:paddingHorizontal="20dp"
            android:text="@string/build_info"
            android:textAllCaps="false"
            android:textColor="@color/text_primary" />
    </LinearLayout>

    <EditText
        android:id="@+id/welcome_text"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="12dp"
        android:background="@drawable/tile_selector"
        android:hint="@string/welcome_text"
        android:importantForAutofill="no"
        android:inputType="textCapSentences"
        android:maxLength="80"
        android:paddingHorizontal="16dp"
        android:singleLine="true"
        android:textColor="@color/text_primary"
        android:textColorHint="@color/text_secondary"
        android:nextFocusUp="@id/save" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal">

        <CheckBox
            android:id="@+id/show_wifi_name"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:buttonTint="@color/focus_blue"
            android:focusable="true"
            android:text="@string/show_wifi_name"
            android:textColor="@color/text_primary"
            android:textSize="17sp" />

        <CheckBox
            android:id="@+id/show_location"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:buttonTint="@color/focus_blue"
            android:focusable="true"
            android:text="@string/show_location"
            android:textColor="@color/text_primary"
            android:textSize="17sp" />

        <CheckBox
            android:id="@+id/show_vpn_status"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:buttonTint="@color/focus_blue"
            android:focusable="true"
            android:text="@string/show_vpn_status"
            android:textColor="@color/text_primary"
            android:textSize="17sp" />

        <CheckBox
            android:id="@+id/show_system_stats"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:buttonTint="@color/focus_blue"
            android:focusable="true"
            android:text="@string/show_system_stats"
            android:textColor="@color/text_primary"
            android:textSize="17sp" />
    </LinearLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/favorite_apps"
        android:textColor="@color/text_secondary"
        android:textSize="18sp"
        android:textStyle="bold" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/available_apps"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="4dp"
        android:layout_weight="1" />
</LinearLayout>
